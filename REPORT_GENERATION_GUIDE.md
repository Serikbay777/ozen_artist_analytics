# 📊 Руководство по генерации PDF-отчетов для артистов

## ✨ Новая функция: Автоматическая генерация отчетов

Система теперь умеет автоматически создавать профессиональные PDF-отчеты для артистов по запросу через AI-агента.

---

## 🎯 Как использовать

### Через AI-агента (рекомендуется)

Просто задайте вопрос агенту:

```
"Сделай отчет для артиста Darkhan Juzz"
"Создай PDF-отчет для Yenlik"
"Квартальный отчет для артиста Mona Songz"
"Сформируй отчет по артисту Ernar Amandyq"
```

Агент автоматически:
1. **Распознает запрос** на генерацию отчета
2. **Выберет нужную тулу** (`generate_artist_report`)
3. **Соберет данные** артиста из базы
4. **Создаст PDF** с полным анализом
5. **Вернет ссылку** на готовый файл

---

## 📄 Что включает отчет

### 1. **Заголовок**
- Имя артиста
- Период отчета (Q4 2025)
- Дата формирования
- Контактная информация компании

### 2. **Ключевые показатели (KPI)**
- Общие стримы
- Общий доход (Believe)
- Заработок артиста (74.8%)
- Уникальных треков
- Охват стран
- Количество платформ
- Средняя цена за стрим

### 3. **Топ треки**
- Название трека
- Количество стримов
- Доход в EUR
- % от общего дохода

### 4. **Анализ платформ (Топ-5)**
- Название платформы
- Стримы
- Доход
- Средняя цена за стрим

### 5. **География (Топ-10 стран)**
- Название страны
- Стримы
- Доход
- % от общих стримов

### 6. **Визуализация**
- 📊 Пай-чарт топ-5 платформ
- 📊 Бар-чарт топ-8 стран по доходу

---

## 🛠️ Техническая архитектура

### Компоненты системы

```
app/tools/report_generation_tools.py     ← Новая тула для агента
app/utils/artist_report_generator.py      ← Генератор PDF-отчетов
reports/artist_reports/                   ← Папка с созданными отчетами
```

### Класс: `GenerateArtistReportTool`

**Наследуется от:** `BaseTool`

**Параметры:**
- `artist_name` (обязательный) - Имя артиста
- `period` (опциональный) - Период отчета (по умолчанию "Q4 2025")
- `include_medialand` (опциональный) - Включить данные Medialand

**Возвращает:**
```python
{
    "success": True,
    "artist_name": "Darkhan Juzz",
    "pdf_path": "/path/to/report.pdf",
    "pdf_filename": "Darkhan_Juzz_Report_20260204_173424.pdf",
    "summary": "📊 Основные показатели: ..."
}
```

---

## 🔧 Класс: `ArtistReportGenerator`

### Основные методы

#### `generate_report(artist_name, period, include_medialand)`
Главный метод генерации отчета

#### `_collect_artist_data(artist_name, period)`
Собирает все данные артиста из `AnalyticsService`:
- Общая статистика
- Топ-платформы
- География
- Топ-треки
- Динамика по месяцам

#### `_create_pdf(data, period, include_medialand)`
Создает PDF-файл с использованием ReportLab:
- Настройка шрифтов (кириллица)
- Форматирование таблиц
- Добавление графиков

---

## 📊 Пример использования

### Через Python API

```python
from app.services.analytics_service import AnalyticsService
from app.utils.artist_report_generator import ArtistReportGenerator

# Инициализация
analytics = AnalyticsService()
generator = ArtistReportGenerator(analytics)

# Генерация отчета
result = generator.generate_report(
    artist_name="Darkhan Juzz",
    period="Q4 2025",
    include_medialand=False
)

if result['success']:
    print(f"PDF создан: {result['pdf_path']}")
    print(result['summary'])
else:
    print(f"Ошибка: {result['error']}")
```

### Через Tool Agent

```python
from app.tools.report_generation_tools import GenerateArtistReportTool

tool = GenerateArtistReportTool()
result = tool.execute(artist_name="Yenlik")
print(result['message'])
```

---

## 📁 Структура файлов

```
reports/
  artist_reports/
    Darkhan_Juzz_Report_20260204_173424.pdf
    Yenlik_Report_20260204_173425.pdf
    Mona_Songz_Report_20260204_173426.pdf
    ...
```

**Формат имени файла:**
```
{Artist_Name}_Report_{Timestamp}.pdf
```

---

## 🎨 Дизайн отчета

### Цветовая схема
- **Заголовки:** #2E3192 (темно-синий)
- **Таблицы:** чередующиеся белый/светло-серый
- **Графики:** яркие контрастные цвета

### Шрифты
1. **Arial Unicode** (macOS) - основной
2. **DejaVu Sans** - резервный
3. **Helvetica** - если нет кириллицы

---

## 🔍 Поиск артиста

Система использует **нечувствительный к регистру** и **частичный поиск**:

```python
# Все эти варианты найдут "Darkhan Juzz":
"darkhan juzz"
"Darkhan"
"DARKHAN JUZZ"
"darkhan"
```

---

## ⚙️ Настройки

### Процент заработка артиста
```python
# В artist_report_generator.py
artist_earnings = total_revenue * 0.748  # 74.8%
```

### Количество элементов в топах
- Топ-5 платформ
- Топ-10 стран
- Топ-10 треков

---

## 🚀 Интеграция с агентом

Агент автоматически распознает запросы на генерацию отчетов благодаря описанию тулы:

```python
@property
def description(self) -> str:
    return """Создать профессиональный PDF-отчет для артиста. 
    Используй этот инструмент когда пользователь просит:
    - 'Сделай отчет для артиста X'
    - 'Создай PDF-отчет для X'
    - 'Квартальный отчет для артиста X'
    - 'Сформируй отчет по артисту X'
    """
```

---

## 📝 Пример успешного теста

```bash
✅ УСПЕХ!
   Артист: Darkhan Juzz
   PDF файл: Darkhan_Juzz_Report_20260204_173424.pdf
   Путь: /Users/.../reports/artist_reports/Darkhan_Juzz_Report_20260204_173424.pdf

📊 Краткая сводка:
📊 **Основные показатели:**
- Всего стримов: 7 353 152
- Общий доход: €2 225.07
- Заработок артиста: €1 664.35
- Уникальных треков: 34

🎵 **Топ трек:** Úıde (2 193 024 стримов)
📱 **Топ платформа:** Spotify (31.55% дохода)
```

---

## 🔧 Устранение неполадок

### Ошибка: "Артист не найден"
- Проверьте написание имени артиста
- Убедитесь что данные артиста есть в базе
- Попробуйте использовать частичное имя

### Ошибка: "Шрифты без кириллицы"
```bash
# Установите DejaVu Sans на macOS:
brew install --cask font-dejavu
```

### PDF не открывается
- Убедитесь что файл полностью создан
- Проверьте права доступа к папке `reports/artist_reports/`

---

## 📚 Связанные файлы

- `scripts/generate_mona_songz_pdf.py` - Оригинальный скрипт (шаблон)
- `scripts/generate_believe_artists_report.py` - Сбор данных всех артистов
- `app/services/analytics_service.py` - Сервис аналитики

---

## ✅ Готово!

Система полностью интегрирована и готова к использованию. Просто спросите агента:

**"Сделай отчет для артиста [Имя]"**

И получите профессиональный PDF-отчет! 🎉

